DATABASE_URL="postgresql://user:password@localhost:5432/resttwin"

NEXTAUTH_SECRET="change-me-to-a-random-32-char-string"
NEXTAUTH_URL="http://localhost:3000"

# LLM provider — set ANTHROPIC_API_KEY to enable real narrative generation
# Without it the mock provider returns deterministic template text
ANTHROPIC_API_KEY=""
